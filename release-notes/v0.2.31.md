# 0.2.31 (2026-04-27)

這個版本重寫專案文件，讓 README、`docs/`、agent guidance、CI/release notes 對齊目前 `main` 的實際架構。

## 文件

- 重寫根目錄 `README.md`，移除舊版號與舊 reader controller 描述
- 重寫 `CLAUDE.md` 與 `CONTRIBUTING.md`，更新目前開發指令、架構邊界與 reader 主線
- 重寫 `docs/architecture.md`，明確記錄 Provider / ChangeNotifier、get_it、Drift、Navigator、flutter_js 的實際接線
- 重寫 `docs/DATABASE.md`，確認資料庫已重置為 Drift schema version `1`
- 重寫 `docs/reader_runtime.md` 與 `docs/reader_spec.md`，改以目前接進 `ReaderPage` 的 `ReaderRuntime` 主線為準
- 重寫 `docs/release.md` 與 `.github/workflows/README.md`，同步目前 CI、Android signing、iOS unsigned IPA 與 tag release 流程

## 清理

- 移除過時的 `docs/reader_next_stage_execution.md`
- 移除過時的 `docs/read_book_controller_decomposition.md`
- 更新 GitHub issue template 中過期的版本與功能提案描述

## 驗證

- `flutter analyze`
- `flutter test`
