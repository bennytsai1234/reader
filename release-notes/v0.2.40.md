# v0.2.40

- 修正遠端目錄為空、章節不存在或正文載入失敗時，不再把空白內容或錯誤訊息當成正式閱讀內容。
- 防止清除 layout cache 後，舊的 in-flight layout task 回填新的 PageResolver cache。
- 修正 slide 模式滑向 loading/error placeholder 時只觸發鄰章預載並回彈，不把 placeholder 當成可落定頁。
- 強化閱讀進度 flush 的 latest-wins 行為，避免 active flush 期間的新位置被漏寫。
